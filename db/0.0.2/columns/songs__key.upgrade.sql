DO
$$
BEGIN

IF NOT EXISTS (SELECT 1 FROM INFORMATION_SCHEMA.COLUMNS
               WHERE TABLE_NAME = 'songs' and COLUMN_NAME = 'key') THEN
    ALTER TABLE songs
        ADD COLUMN key text DEFAULT NULL;
END IF;

END;
$$